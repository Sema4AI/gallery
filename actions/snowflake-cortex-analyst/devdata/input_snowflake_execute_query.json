{
    "inputs": [
        {
            "inputName": "input-1",
            "inputValue": {
                "query": "SELECT * FROM daily_revenue LIMIT 10",
                "warehouse": "COMPUTE_WH",
                "database": "CORTEX_ANALYST_DEMO",
                "schema": "REVENUE_TIMESERIES",
                "numeric_args": []
            }
        }, {
            "inputName": "input-error-warehouse",
            "inputValue": {
                "query": "SELECT * FROM daily_revenue LIMIT 10",
                "warehouse": "XXX",
                "database": "CORTEX_ANALYST_DEMO",
                "schema": "REVENUE_TIMESERIES",
                "numeric_args": []
            }
        }, {
            "inputName": "input-error-database",
            "inputValue": {
                "query": "SELECT * FROM daily_revenue LIMIT 10",
                "warehouse": "COMPUTE_WH",
                "database": "XXX",
                "schema": "REVENUE_TIMESERIES",
                "numeric_args": []
            }
        }, {
            "inputName": "input-error-schema",
            "inputValue": {
                "query": "SELECT * FROM daily_revenue LIMIT 10",
                "warehouse": "COMPUTE_WH",
                "database": "CORTEX_ANALYST_DEMO",
                "schema": "XXX",
                "numeric_args": []
            }
        }, {
            "inputName": "input-error-query",
            "inputValue": {
                "query": "SELECT * FROM XXXX LIMIT 10",
                "warehouse": "COMPUTE_WH",
                "database": "CORTEX_ANALYST_DEMO",
                "schema": "REVENUE_TIMESERIES",
                "numeric_args": []
            }
        }, {
            "inputName": "input-with-custom-row-limit",
            "inputValue": {
                "query": "SELECT * FROM daily_revenue",
                "warehouse": "COMPUTE_WH",
                "database": "CORTEX_ANALYST_DEMO",
                "schema": "REVENUE_TIMESERIES",
                "numeric_args": [],
                "row_limit": 4
            },
            "comment": "Example with custom row limit to prevent memory issues on large result sets"
        }
    ],
    "metadata": {
        "actionName": "snowflake_execute_query",
        "actionRelativePath": "snowflake_actions.py",
        "schemaDescription": [
            "query: string: The query to execute.",
            "numeric_args: string: A list of numeric arguments to pass to the query.",
            "row_limit: integer: Maximum number of rows to return (default: 10000)."
        ],
        "managedParamsSchemaDescription": {
            "warehouse": {
                "type": "Secret",
                "description": "Your Snowflake virtual warehouse to use for queries."
            },
            "database": {
                "type": "Secret",
                "description": "Your Snowflake database to use for queries."
            },
            "schema": {
                "type": "Secret",
                "description": "Your Snowflake schema to use for queries."
            }
        },
        "inputFileVersion": "v3",
        "kind": "action",
        "actionSignature": "action/args: 'query: str, warehouse: Secret, database: Secret, schema: Secret, numeric_args: list=None, row_limit: int=10000'"
    }
}